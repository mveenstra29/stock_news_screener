"""Tests for narrator.py.

narrator.py is currently a stub (see src/portfolio_narrator/narrator.py).
Write real tests here once write_narrative is implemented — e.g. mock the
anthropic client and assert the prompt only ever includes pre-computed
numbers from calculations.py, never numbers the LLM invents itself.
"""

import pytest


@pytest.mark.skip(reason="narrator.py is not implemented yet")
def test_write_narrative():
    pass
